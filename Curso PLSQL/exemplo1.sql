SELECT
  (apresentar)
  FROM
  (tabela principal)
  JOIN
  (tabelas complementares)
 WHERE
  (filtro)
 GROUP BY
  (agrupar)
HAVING
  (filtro agrupamento)
 ORDER BY
  (ordenação)
 LIMIT
  (limitar registros)